# Postgres
POSTGRES_DB=alcorzop
POSTGRES_USER=alcorzop
POSTGRES_PASSWORD=alcorzop
DATABASE_URL=postgresql://alcorzop:alcorzop@db:5432/alcorzop?schema=public

# Admin user (seeded)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me

# Optional obfuscated admin path (default: /admin)
ADMIN_PATH=/admin

# Session TTL (days)
SESSION_TTL_DAYS=7

# File uploads
UPLOAD_DIR=/data/uploads
MAX_UPLOAD_MB=20
